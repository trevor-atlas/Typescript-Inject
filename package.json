{
  "name": "dependency-injection",
  "version": "1.0.0",
  "description": "An example of DI in a typescript/node project",
  "main": "build/app.js",
  "engines": {
    "node": ">= 10.x"
  },
  "scripts": {
    "dev": "npm run build && export ENV=dev && node build/app.js",
    "prod": "npm run build && export ENV=production && node build/app.js",
    "build": "rm -rf ./build && npx tsc"
  },
  "author": "Trevor Atlas - trevoratlas.com",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.12"
  },
  "dependencies": {
    "typescript": "^3.5.2"
  }
}
